package eclipse.model.xml;

/**
 * Permet de loader le protocol XMl
 * 
 * L'interface will be usefull in further eclipse version, so package xml8 can be easely replace by xml9 or 10 or whatever or if there is a new protocol 
 * @author Marco
 *
 */
public interface ProtocolLoader {
	public void load();
}
